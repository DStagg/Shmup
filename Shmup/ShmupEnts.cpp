#include "ShmupEnts.h"

PlayerEnt::PlayerEnt()
{
	
};

void PlayerEnt::Update(float dt)
{
	/*
	SetX(GetX() + (GetXVel()*dt));
	SetY(GetY() + (GetYVel()*dt));

	if ( GetIcon()->GetWidth() != 0 )
		SetWidth(GetIcon()->GetWidth());
	if ( GetIcon()->GetHeight() != 0)
		SetHeight(GetIcon()->GetHeight());
	*/
};